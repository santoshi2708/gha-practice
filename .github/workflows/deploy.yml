name: test-activities
on:
  pull_request:
    types: [opened,closed]
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: get code
        run: actions/checkout@v3
      - name: set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
      - name: deploy
        run: echo "Deploying..."